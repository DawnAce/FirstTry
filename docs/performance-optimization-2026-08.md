# 发行系统性能优化记录（2026-08）

## 背景与结论

生产数据量不大，页面慢的主要原因不是表规模，而是应用连接公网 MySQL 后，每一次连接和 SQL 往返都很昂贵。优化前首次连接约 13 秒，复用连接上的单次查询也常需 0.6–2.3 秒；因此逐行查询和页面并发请求会被网络延迟成倍放大。

本轮保持业务口径不变，优先消除 N+1、重复请求和不必要的连接创建。数据库迁移不在本轮范围内。

## 基线与复测

| 场景 | 优化前 | 优化后 |
|---|---:|---:|
| 刊期列表（历史印数期数） | 9 期共 20 次 SQL；HTTP 约 11.9 秒 | 1 次 SQL；远程数据库复用连接约 0.65 秒 |
| Dashboard | HTTP 约 15.2 秒 | 3 次 SQL；复用连接约 1.6 秒 |
| 订单列表 | 2 单共 9 次 SQL；页面 7 个并发请求，约 9.1 秒 | 列表和六个视图数量合并为 1 个请求、约 3 次 SQL；HTTP 抽样约 3–6 秒 |
| 报数确认 | 保存后确认两次请求；32 条明细逐条查询 | 当前值随确认一次提交；明细一次加载并在同一事务更新 |
| 物流跨期总览 | 6 次 SQL；约 3.8–5.1 秒 | 2 次 SQL；约 1.58–1.59 秒 |
| 邮局投递 / 工单 / 收款 | 4–5 次 SQL；约 3.8–4.3 秒 | 2 次 SQL；约 1.0–1.6 秒 |
| 订单详情主体 | 12 次 SQL；约 7.2 秒 | 5 次 SQL；约 3.94 秒 |

远程网络存在明显波动，上表时间用于判断数量级，不作为 SLA。SQL 次数是更稳定的回归指标：列表行数增加时，查询次数不应线性增长。

## 已落地的优化

### 后端

- 历史印数期数列表将刊期、年内期次、计划版数和印数合计合并为一次数据库查询；查询次数不再随刊期数增长。
- Dashboard 一次加载出版计划，在内存计算年内期次、计划版数和下一期。
- 订单列表批量加载刊期、商学院刊期和发货进度；`include_view_counts=true` 时，列表、分页总数和六个状态数量共享一次聚合。
- 保留 `GET /api/orders/view-counts` 作为兼容接口；发行系统前端使用合并后的订单列表响应。
- 邮局投递批量诊断订单、明细和最新收件目标；概览的份数、单位数、缺失数和最近到期日合并为一次聚合。
- 报数确认可接收当前 `entries`，保存、校验、快照和确认在同一事务完成。
- 启动时并发预热 6 条核心数据库连接；连接池默认大小 10、溢出 20，使用 LIFO 和 1 小时回收。
- 关闭逐请求 `pool_pre_ping`，避免每个 API 先增加一次公网 SQL 往返；一小时主动回收负责处理常规空闲连接寿命。
- 已认证用户验证结果缓存 5 分钟，并防止 React StrictMode 重复请求 `/auth/me`。
- 邮局投递 5→2 SQL，邮局工单 4→2 SQL，邮局收款列表 5→2 SQL、入口摘要 1 SQL。
- 物流总览 6→2 SQL；单期报数 5→2/3 SQL；报数来源摘要 4→2 SQL；履约摘要 7→5 SQL。
- 合同和渠道结算消除按行读取渠道/附件的 N+1，分别固定为 1 和 2 SQL。
- 营销交易入口不再读取两个完整订单列表，改用单 SQL 的 `/api/orders/portal-summary`。
- 大响应启用 GZip。所有 API 返回 SQL 与应用耗时的 `Server-Timing`；超过 2 秒记录带查询次数的 `slow_api` 日志。

### 前端

- 所有页面组件按路由懒加载，进入页面时只下载所需 chunk。
- TanStack Query 默认 `staleTime` 调整为 2 分钟、`gcTime` 保持 10 分钟。
- 订单页从六次状态计数加一次列表请求，收敛为一次合并列表请求。
- 登录后等待 10 秒，再以小批次预取全部业务路由 chunk，后续切页无需现场下载且不与当前首屏争抢。
- 业务中心与目标页面统一 TanStack Query key，刚读取过的数据可直接复用。
- 印数 Dashboard 的折线图改为原生 SVG，页面生产 chunk 从约 1.15 MB 降到约 15.2 KB。
- ZTO-MF 单期详情只请求一次全量明细，筛选和公司选项在本地派生，重试不重复刷新同一资源。
- 报数确认直接提交编辑器中的当前值，不再先调用独立保存接口。

## 接口与运维约定

- `GET /api/orders?include_view_counts=true` 的响应除 `rows`、`total` 外还包含 `view_counts`：`all`、`active`、`pending_confirmation`、`draft`、`attention`、`void`。
- 角色或用户删除最多需要 5 分钟反映到已登录请求，这是认证缓存的明确边界。
- `Server-Timing` 现在返回 `db;dur=...;desc="N queries", app;dur=...`；观察慢请求时先区分 SQL 数量、SQL 耗时和应用处理。
- 不要把批量序列化重新改成循环内查询，也不要恢复订单页的多状态并发请求。
- 若目标是稳定 1 秒级，优先将后端与 MySQL 放在同地域内网，或使用受控的数据库连接代理；应用层继续堆缓存无法消除公网握手延迟。

## 验证

- 后端完整测试：654 passed，2 skipped。
- 前端单元测试：137 passed；生产构建通过。
- 前端 lint：0 errors；已有 warnings 不阻断构建。
- `git diff --check`：通过。
- 全路由覆盖与逐页 SQL 上限见 `docs/page-loading-audit-2026-08.md`。
